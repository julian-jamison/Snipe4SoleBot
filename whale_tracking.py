"""
whale_tracking.py
~~~~~~~~~~~~~~~~~

Placeholder module.

`get_whale_transactions(token_address)` returns a tuple
    (total_whale_buys_SOL, total_whale_sells_SOL)

Right now it just returns (0, 0).  Replace with real API logic later.
"""

from __future__ import annotations
from typing import Tuple

async def get_whale_transactions(token_address: str) -> Tuple[float, float]:
    # stub: no whale data available
    return 0.0, 0.0
